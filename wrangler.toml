name = "eiger-marvel-hr-plat"
compatibility_date = "2024-01-01"

# Cloudflare Pages configuration
pages_build_output_dir = "dist"

# Optional custom domain route (requires domain verification in Cloudflare)
# Uncomment when domain is properly configured
# [[routes]]
# pattern = "emgc.sgctech.ai/*"
# zone_name = "sgctech.ai"
